---
title: LangGraph.js client-tool helpers
description: Generated API reference for bindClientTools() and the other routing helpers exported from @threadplane/middleware/langgraph.
---

# LangGraph.js client-tool helpers

`bindClientTools()` is the main helper from `@threadplane/middleware/langgraph`. It binds your server tools plus the current run's client-tool catalog onto a LangChain chat model.

```ts
import {
  bindClientTools,
  clientToolsChannel,
  clientToolsRouter,
} from '@threadplane/middleware/langgraph';
```

There is no root JavaScript entry point for `@threadplane/middleware`; import from the `/langgraph` subpath. The Python package exposes equivalent snake_case helpers from `threadplane.middleware.langgraph`.

The routing helpers in the same entry point include `clientToolsChannel()`, `clientToolsRouter()`, `clientToolSpecs()`, `clientToolNames()`, `hasClientToolCall()`, `hasServerToolCall()`, `routeAfterAgent()`, and `lastMessage()`. The entry point also exports client-tool execution stores; [Introduction](/docs/middleware/getting-started/introduction) lists the supported surface and explains the breaking removal of the receipt helpers.

{/* Auto-rendered from api-docs.json — see page component */}
